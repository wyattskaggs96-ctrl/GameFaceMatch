# Demo Data

Reserved namespace for future non-production demo data.

- `sourceType` must be `demoData`.
- Demo data must never be imported into `data/catalog/production/`.
- Do not add real College Football 27 options here.
