# Duplicate Intake

Use this folder only after a human review confirms that an intake file is an exact duplicate or otherwise belongs in duplicate handling.

The intake command identifies exact hash matches but does not delete or move duplicates automatically.
