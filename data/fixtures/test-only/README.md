# Test-Only Fixtures

All records here are synthetic and must be visibly marked as fixtures.

Application production builds must reject these records.

Fixture catalog-like records must use `sourceType: "testFixture"` and must never be copied into `data/catalog/production/`.
