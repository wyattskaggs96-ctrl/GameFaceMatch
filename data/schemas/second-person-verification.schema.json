{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://gamefacematch.local/schemas/second-person-verification.schema.json",
  "title": "Phase0SecondPersonVerificationRecord",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "verificationID",
    "targetStableID",
    "verificationScope",
    "primaryObservation",
    "verifierObservation",
    "evidenceExists",
    "frontViewExists",
    "secondaryAngleSampleIncluded",
    "randomizationMethod",
    "discrepancyType",
    "resolutionAction",
    "resolutionEvidenceIDs",
    "primaryAcknowledgedAt",
    "verifierAcknowledgedAt",
    "finalDisposition",
    "notes"
  ],
  "properties": {
    "schemaVersion": { "const": "phase0-second-person-verification-v1" },
    "verificationID": { "$ref": "#/$defs/id" },
    "targetStableID": { "$ref": "#/$defs/text" },
    "verificationScope": { "$ref": "#/$defs/verificationScope" },
    "primaryObservation": { "$ref": "#/$defs/observation" },
    "verifierObservation": { "$ref": "#/$defs/observation" },
    "evidenceExists": { "type": "boolean" },
    "frontViewExists": { "type": "boolean" },
    "secondaryAngleSampleIncluded": { "type": "boolean" },
    "randomizationMethod": { "$ref": "#/$defs/text" },
    "discrepancyType": { "$ref": "#/$defs/discrepancyType" },
    "resolutionAction": { "$ref": "#/$defs/resolutionAction" },
    "resolutionEvidenceIDs": { "type": "array", "items": { "$ref": "#/$defs/id" } },
    "primaryAcknowledgedAt": { "type": ["string", "null"], "format": "date-time" },
    "verifierAcknowledgedAt": { "type": ["string", "null"], "format": "date-time" },
    "finalDisposition": { "$ref": "#/$defs/approvedVerificationStatus" },
    "notes": { "$ref": "#/$defs/text" }
  },
  "$defs": {
    "id": { "type": "string", "minLength": 1 },
    "text": { "type": "string", "minLength": 1 },
    "approvedVerificationStatus": {
      "enum": [
        "VERIFIED",
        "VERIFIED_WITH_NOTES",
        "RECAPTURE_REQUIRED",
        "VERSION_MISMATCH",
        "MISSING_EVIDENCE",
        "COUNT_MISMATCH",
        "ORDER_MISMATCH",
        "DEPENDENCY_UNRESOLVED",
        "NOT_VERIFIED"
      ]
    },
    "verificationScope": { "enum": ["catalogItem", "menuMap", "evidenceFile", "captureLog", "catalogPackage"] },
    "discrepancyType": {
      "enum": [
        "none",
        "labelMismatch",
        "versionMismatch",
        "missingEvidence",
        "countMismatch",
        "orderMismatch",
        "dependencyUnresolved",
        "captureQuality",
        "menuNavigationMismatch",
        "other"
      ]
    },
    "resolutionAction": {
      "enum": [
        "acceptPrimaryObservation",
        "acceptVerifierObservation",
        "recaptureEvidence",
        "splitByVersion",
        "correctDraftRecord",
        "markNotVerified",
        "holdForResearch",
        "retireRecord"
      ]
    },
    "observation": {
      "type": "object",
      "additionalProperties": false,
      "required": ["observerID", "observedAt", "summary", "evidenceIDs"],
      "properties": {
        "observerID": { "$ref": "#/$defs/text" },
        "observedAt": { "type": "string", "format": "date-time" },
        "summary": { "$ref": "#/$defs/text" },
        "evidenceIDs": { "type": "array", "minItems": 1, "items": { "$ref": "#/$defs/id" } }
      }
    }
  }
}
